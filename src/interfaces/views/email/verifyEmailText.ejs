Thanks for registering with <%= brand %>.

Your username is <%= username %>.

You just need to verify your email address in order to use the service.

Follow https://<%= fqdn %>/verify/<%= token %> to verify your account.

This link will only be valid for the next 12 hours and will only work once.

Too late? Browse to https://<%= fqdn %>/verify-resend to generate a new token.
